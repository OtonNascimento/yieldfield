# shared

Cross-feature, non-visual app code (§14). Holds the generated API client wiring,
generic hooks, pure helpers (money/date formatting), and app-level types. No UI
primitives and no business rules live here (§6.2, §7).
