// Chart primitives (AreaChart, BarChart, Gauge) are ported from the delivered
// charts.jsx as typed, token-colored React in Slice 4 (§5A). Charts are an in-repo
// concern — never a third-party chart library. Empty for now.
export {};
