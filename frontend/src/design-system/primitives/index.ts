// Visual primitives (Card, Button, Chip, Tag, Input, Segmented, Drawer, Timeline,
// Meter, …) are ported from the delivered references in Slice 4 (§5A). This is the
// only home for them; features compose these and never define their own. Empty for now.
export {};
